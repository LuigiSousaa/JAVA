package com.projeto.View;

import javax.swing.*;
import java.awt.*;

public class LoginCliente extends JPanel {
    public LoginCliente() {
        setLayout(new BorderLayout());
        // Adicione os componentes necessários para o login de clientes aqui
        add(new JLabel("Página de Login do Cliente"), BorderLayout.CENTER);
    }
}