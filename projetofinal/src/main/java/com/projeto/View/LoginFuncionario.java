package com.projeto.View;

import javax.swing.*;
import java.awt.*;

public class LoginFuncionario extends JFrame {
    public LoginFuncionario() {
        setLayout(new BorderLayout());
        // Adicione os componentes necessários para o login de funcionários aqui
        add(new JLabel("Página de Login do Funcionário"), BorderLayout.CENTER);
    }
}